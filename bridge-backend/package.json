{
  "name": "bridge-backend",
  "version": "0.1.0",
  "description": "Project BRIDGE — application backend (accounts, store, inventory, codes, admin)",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  }
}
